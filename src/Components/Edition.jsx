import React from 'react'
import { feedback } from '../constants'
import styles from '../style'

const Testimonials = () => {
  return (
  <section id="clients" className={`${styles.paddingY} ${styles.flexCenter} flex-col 
  relative pt-[200px] text-[#fff]`}>
  <div className='flex flex-col'>
  <div className='flex flex-col'>
    <p className='text-[94px] leading-[-10px] tracking-tighter' style={{fontWeight: 900}}>
       We are setting the standard <br/> for DevFest Student Edition!
    </p>
 </div>   

  <div>
    
  </div>
    
  </div>
  </section>
  )
}

export default Testimonials